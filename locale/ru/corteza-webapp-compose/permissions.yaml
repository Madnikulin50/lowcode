ui:
  save: Сохранить изменения
  cancel: Отменить
  set-for: Установить права доступа для {{target}}
  loading: Загрузка прав доступа
  label: Права доступа

  notification:
    save:
      success: Права доступа saved
      failed: Failed to save permissions

  edit:
    title: Изменить права доступа
    label: Роли пользователя
    description: Выберите роль для установки прав

  evaluate:
    title: Вычислить права доступа
    description: Evaluate applied permissions

  add:
    label: Добавить
    title: Вычислить права доступа
    save: Сохранить и закрыть
    role:
      label: Роли
      placeholder: Выбрать роли
    user:
      label: Пользователи
      placeholder: Поиск или выбор пользователя

  tooltip:
    unknown-context:
      user: Can not evaluate permission for a user with contextual role on a non-specific resource
      role: Can not evaluate permission for contextual role on a non-specific resource

  access:
    allow: Разрешено
    inherit: Наследовано
    deny: Запрещено

resources:
  compose:
    namespace:
      all: все пространства
      specific: namespace "{{target}}"
      tooltip: Пространство permissions
      operations:
        read:
          description: ""
          specific: Read namespace "{{target}}"
          title: Read any namespace
        update:
          description: ""
          specific: Update namespace "{{target}}"
          title: Update any namespace
        delete:
          description: ""
          specific: Удалить namespace "{{target}}"
          title: Удалить any namespace
        manage:
          description: Allow access to namespace's admin pages
          specific: Manage namespace "{{target}}"
          title: Manage any namespace
        export:
          description: ""
          specific: Экспорт namespace "{{target}}"
          title: Экспорт any namespace
        module.create:
          description: ""
          specific: Create modules under namespace "{{target}}"
          title: Create modules under any namespace
        modules.export:
          description: ""
          specific: Экспорт modules in namespace "{{target}}"
          title: Экспорт modules in any namespace
        modules.search:
          description: ""
          specific: Список and search modules under namespace "{{target}}"
          title: Список and search modules under any namespace
        page.create:
          description: ""
          specific: Create pages under namespace "{{target}}"
          title: Create pages under any namespace
        pages.search:
          description: ""
          specific: Список and search pages under namespace "{{target}}"
          title: Список and search pages under any namespace
        pages.export:
          description: ""
          specific: Экспорт pages in namespace "{{target}}"
          title: Экспорт pages in any namespace
        chart.create:
          description: ""
          specific: Create charts under namespace "{{target}}"
          title: Create charts under any namespace
        charts.search:
          description: ""
          specific: Список and search charts under namespace "{{target}}"
          title: Список and search charts under any namespace
        charts.export:
          description: ""
          specific: Экспорт charts in namespace "{{target}}"
          title: Экспорт charts in any namespace

    chart:
      all: all charts
      specific: chart "{{target}}"
      tooltip: Диаграмма permissions
      operations:
        delete:
          description: 'Default: deny'
          specific: Удалить chart "{{target}}"
          title: Удалить any chart
        read:
          description: 'Default: deny'
          specific: Read chart "{{target}}"
          title: Read any chart
        update:
          description: 'Default: deny'
          specific: Update chart "{{target}}"
          title: Update any chart

    module:
      all: all modules
      specific: module "{{target}}"
      tooltip: Права доступа к модулю
      operations:
        delete:
          description: 'Default: deny'
          specific: Удалить module "{{target}}"
          title: Удалить any module
        read:
          description: 'Default: deny'
          specific: Read module "{{target}}"
          title: Read any module
        record.create:
          description: 'Default: deny'
          specific: Create record of module "{{target}}"
          title: Create record of any module
        records.search:
          description: 'Default: deny'
          specific: Список and search records of module "{{target}}"
          title: Список and search records of any module
        update:
          description: 'Default: deny'
          specific: Update module "{{target}}"
          title: Update any module
        'owned-record.create':
          description: 'Default: deny'
          specific: Manage owner when creating запись для модуля "{{target}}"
          title: Manage owner when creating record for any module
          all-specific: Manage owner when creating запись для модуля "{{ target }}"

    module-field:
      all: all module fields
      specific: field "{{target}}"
      all-specific: all module fields of module "{{ target }}"
      tooltip: Module field permissions
      operations:
        record.value.read:
          description: 'Default: deny'
          specific: 'Read "{{target}}" record fields '
          title: Read record fields of any module
          all-specific: Read record fields of module "{{ target }}"
        record.value.update:
          description: 'Default: deny'
          specific: 'Update "{{target}}" record field values'
          title: Update record field values of any module
          all-specific: Update record field values of module "{{ target }}"

    page:
      all: all pages
      specific: page "{{target}}"
      tooltip: Page permissions
      operations:
        delete:
          description: 'Default: deny'
          specific: Удалить page "{{target}}"
          title: Удалить any page
        read:
          description: 'Default: deny'
          specific: Read page "{{target}}"
          title: Read any page
        update:
          description: 'Default: deny'
          specific: Update page "{{target}}"
          title: Update any page
        page-layout.create:
          description: 'Default: deny'
          specific: Create page layouts for page "{{target}}"
          title: Create page layout
        page-layouts.search:
          description: 'Default: deny'
          specific: Список page layouts for page "{{target}}"
          title: Список page layout

    page-layout:
      all: all page layouts
      specific: page layout "{{target}}"
      tooltip: Page layout permissions
      all-specific: all page layouts of page "{{ target }}"
      operations:
        read:
          description: 'Default: deny'
          specific: Read page layout "{{target}}"
          title: Read any page layout
          all-specific: Read page layouts of page "{{ target }}"
        update:
          description: 'Default: deny'
          specific: Update page layout "{{target}}"
          title: Update any page layout
          all-specific: Update page layouts of page "{{ target }}"
        delete:
          description: 'Default: deny'
          specific: Удалить page layout "{{target}}"
          title: Удалить any page layout
          all-specific: Удалить page layouts of page "{{ target }}"

    record:
      all: all records
      specific: record "{{target}}"
      all-specific: all records of module "{{ target }}"
      tooltip: Права доступа к записи
      operations:
        delete:
          description: 'Default: deny'
          specific: Удалить record "{{target}}"
          title: Удалить records of any module
          all-specific: Удалить records of module "{{ target }}"
        undelete:
          description: 'Default: deny'
          specific: Restore record "{{target}}"
          title: Restore records of any module
          all-specific: Restore records of module "{{ target }}"
        read:
          description: 'Default: deny'
          specific: Read record "{{target}}"
          title: Read records of any module
          all-specific: Read records of module "{{ target }}"
        update:
          description: 'Default: deny'
          specific: Update record "{{target}}"
          title: Update records of any module
          all-specific: Update records of module "{{ target }}"
        'owner.manage':
          description: 'Default: deny'
          specific: Manage owner of record "{{target}}"
          title: Manage record owners of any module
          all-specific: Manage record owners of module "{{ target }}"
        'revisions.search':
          description: 'Default: deny'
          specific: Access revisions of record "{{target}}"
          title: Access record revisions
          all-specific: Access record revisions
