version: '3.5'

services:


  server:
    image: pnp-lowcode:${VERSION}
    restart: always
    extra_hosts:
      - "host.docker.internal:192.168.173.74"
    env_file: [ .env ]
    ports:
      - "${HTTP_PORT}:80"
    volumes:
      - ./data/server:/data

