
services:
  server:
    image: docker.io/madnikulin50/pnp-lowcode:${VERSION}
    restart: always
    extra_hosts:
      - "host.docker.internal:${LOCAL}"
    env_file: [ .env ]
    ports:
      - "${HTTP_PORT}:80"
    volumes:
      - ./data/server:/data

